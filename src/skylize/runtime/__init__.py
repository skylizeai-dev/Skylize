"""
The agent sandbox host and tool proxy (IF-TOOL).

Placeholder package — the tool proxy (token validation before every side effect)
lands in Sprint 2's runtime work / Sprint 3 crew execution. Guarded by the
import-linter "no database driver" contract.
"""
