"""
The Memory service (VectorStore + MemoryRepository ports).

Placeholder package — hybrid retrieval and the Qdrant adapter land in Sprint 2's
memory work. Guarded by the import-linter "no database driver" contract (it goes
through the DAL).
"""
