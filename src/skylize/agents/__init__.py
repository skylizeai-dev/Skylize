"""
IF-AGENT: untrusted agent graphs/crews.

Placeholder package — the creative crew implementations land in Sprint 3. The
import-linter contract already guards it: modules here may import ONLY
`skylize.schemas` (no adapters, dal, app, runtime, events, memory).
"""
